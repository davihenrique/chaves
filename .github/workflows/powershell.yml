name: Test PowerShell on Ubuntu
on: push

jobs:
  pester-test:
    name: Pester test
    runs-on: ubuntu-latest
    steps:
      - name: Check out repository code
        uses: actions/checkout@v3
      - name: Perform a Pester test from the Tests.ps1 file
        shell: pwsh
        run: |
        $scripts = Get-ChildItem -Path ./scripts -Recurse | Where-Object {$_.Extension -eq ".ps1"}
        foreach($script in $scripts){ Invoke-Pester ./scripts/ViewCsv.ps1 -Passthru}
          