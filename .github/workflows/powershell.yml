name: powershell

on: [push]

jobs:
    build:
      name: Run Script
      runs-on: windows-latest
      steps:
        - uses: actions/checkout@v1
        - name: Script
          working-directory: .\scripts
          run: MockData.ps1
          shell: powershell